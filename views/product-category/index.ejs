<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no"/>
    <link rel="icon" type="image/png" href="/assets/i/favicon.png">
    <title><%= title %></title>
    <% include ../partials/css %>
</head>
<body>

<% include ../partials/header %>

<div class="am-container common-page">

    <% if (flash.success != '') { %>
    <div class="am-alert am-alert-success">
        <p><%= flash.success %></p>
    </div>
    <% } %>
    
        <fieldset>

            <legend><a href="/product-category">产品分类列表</a></legend>

            <form class="am-form-inline" role="form" action="/product-category">
                <div class="am-form-group">
                  搜索分类标题：<input type="text" name="search-product-category-name" class="am-form-field" placeholder="搜索">
                </div>
                <button type="submit" class="am-btn am-btn-primary">搜索</button>
            </form>

            <hr/>
            
            <table class="am-table">
                <thead>
                <tr>
                    <th>分类标题</th>
                    <th class="small-screen-hide">创建时间</th>
                    <th class="small-screen-hide">更新时间</th>
                    <th>删除</th>
                </tr>
                </thead>
                <tbody>
                <% for(var i=0; i < productCategory.length; i++) {%>
                <tr>
                    <td><a href="/product-category/edit/<%= productCategory[i].get('categoryId') %>"><%= productCategory[i].get('categoryName') %></a> </td>
                    <td class="small-screen-hide"><%=productCategory[i].createdAt.getFullYear()%>-<%=(productCategory[i].createdAt.getMonth()+1)%>-<%=productCategory[i].createdAt.getDate()%></td>
                    <td class="small-screen-hide"><%=productCategory[i].updatedAt.getFullYear()%>-<%=(productCategory[i].updatedAt.getMonth()+1)%>-<%=productCategory[i].updatedAt.getDate()%></td>
                    <td><a class="remove-product-category" data-id="<%= productCategory[i].get('categoryId') %>" href="/product-category/remove/<%= productCategory[i].get('categoryId') %>">删除</a></td>
                </tr>
                <% } %>
                </tbody>
            </table>
            
            <ul class="am-pagination">
                <% if (productCategoryPager.prev) { %> <li>
                    <a href="/product-category?page=<%= productCategoryPager.prev %><% if(searchProductCategoryName) {%>&search-product-category-name=<%=searchProductCategoryName%> <%}%>">上一页</a></li> <% } %>
                <% if (productCategoryPager.next) { %> <li>
                    <a href="/product-category?page=<%= productCategoryPager.next %><% if(searchProductCategoryName) {%>&search-product-category-name=<%=searchProductCategoryName%> <%}%>">下一页</a></li> <% } %>
                <li class="count">第<%=productCategoryPager.page%>页 共<%= productCategoryCount %>条</li>
            </ul>
            
            <hr/>

            <div class="am-form-group">
                <a href="/product-category/add" class="am-btn am-btn-primary" role="button">添加新分类</a>
            </div>

        </fieldset>


</div>


<div class="am-modal am-modal-confirm" tabindex="-1" id="confirm-remove-product-category">
    <div class="am-modal-dialog">
        <div class="am-modal-bd">
            确定要删除这条记录吗？
        </div>
        <div class="am-modal-footer">
            <span class="am-modal-btn" data-am-modal-confirm>确定</span>
            <span class="am-modal-btn" data-am-modal-cancel>取消</span>
        </div>
    </div>
</div>


<div class="am-modal am-modal-alert" tabindex="-1" id="alert-remove-product-category">
    <div class="am-modal-dialog">
        <div class="am-modal-bd">
        </div>
        <div class="am-modal-footer">
            <span class="am-modal-btn">确定</span>
        </div>
    </div>
</div>


<% include ../partials/footer %>
<% include ../partials/js %>

<script type="text/javascript">
    $(function() {
        var productCategory = require('product-category');
        productCategory.indexFun();
    });
</script>
</body>
</html>