<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no"/>
    <link rel="icon" type="image/png" href="/assets/i/favicon.png">
    <title><%= title %></title>
    <% include ../partials/css %>
</head>
<body>

<% include ../partials/header %>

<div class="am-container common-page order-page">

    <% if (flash.success != '') { %>
    <div class="am-alert am-alert-success">
        <p><%= flash.success %></p>
    </div>
    <% } %>
    
        <fieldset>

            <legend><a href="/order">发货订单列表</a></legend>

            <form class="am-form-inline" role="form" action="/order">
                <div class="am-form-group">
                  订单名：<input type="text" name="search-order-name" class="am-form-field" placeholder="搜索">
                </div>
                <span></span>
                <div class="am-form-group">
                    用户名：<input type="text" name="search-customer-name" class="am-form-field" placeholder="搜索">
                </div>
                <span></span>
                <div class="am-form-group">
                    地址：<input type="text" name="search-address" class="am-form-field" placeholder="搜索">
                </div>
                <button type="submit" class="am-btn am-btn-primary">搜索</button>
            </form>

            <hr/>
            
            <table class="am-table">
                <thead>
                <tr>
                    <th>订单名称</th>
                    <th class="t-c">收件人</th>
                    <th class="t-c" width="200">淘宝名</th>
                    <th class="t-c">快递</th>
                    <th class="t-c">状态</th>
                    <th class="t-c small-screen-hide">更新时间</th>
                    <th class="t-c small-screen-hide">删除</th>
                </tr>
                </thead>
                <tbody>
                <% for(var i=0; i < order.length; i++) {%>
                <tr data-customer-id="<%= order[i].get('customerId') %>">
                    <td>
                        <%for(var j=0; j<order[i].get('name').length;j++) {%>
                            <div>
                                <span class="image loading-elem on" data-id="<% if(/\{id\:\d+\}/.test(order[i].get('name')[j])){%><%= order[i].get('name')[j].replace(/([^\{]+\{id\:)(\d+)(\})/,'$2') %><%}%>"></span>
                                <a href="/order/edit/<%= order[i].get('orderId') %>">
                                    <%= order[i].get('shippingCount')[j] %>*<%= order[i].get('name')[j].replace(/\{id\:\d+\}/,'') %>
                                </a>
                                <% if(/\{id\:\d+\}/.test(order[i].get('name')[j])) {%>
                                    <a class="set-stock" title="设置库存" href="javascript:;"><i class="am-icon am-icon-external-link"></i></a>
                                    <a class="update-stock" data-order-id="<%= order[i].get('orderId') %>" data-product-id="<%= order[i].get('name')[j].replace(/([^\{]+\{id\:)(\d+)(\})/,'$2') %>" title="是否已设置库存" href="javascript:;"><i class="am-icon am-icon-check-circle <%if(order[i].get('isShipping')[j]){%>on<%}else{%>off<%}%>"></i></a>
                                <%}%>
                            </div>
                        <%}%>
                    </td>
                    <td class="t-c"><a title="<%= order[i].get('shippingAddress') %>" href="/customer/edit/<%= order[i].get('customerId') %>"><%= order[i].get('customerName') %></a> </td>
                    <td class="t-c"> <span class="taobao-name loading-elem on"></span> </td>
                    <td class="t-c">
                        <a href="http://www.kuaidi100.com/auto.shtml?nu=<%= order[i].get('trackingNumber') %>" target="_blank"><% if(order[i].get('shippingCompany') == 'tiantian') {%>天天<%}%>
                            <% if(order[i].get('shippingCompany') == 'shunfeng') {%>顺丰<%}%>
                            <% if(order[i].get('shippingCompany') == 'baishi') {%>百世汇通<%}%>
                            <% if(order[i].get('shippingCompany') == 'other') {%>其他<%}%>
                        </a>
                    </td>
                    <td class="t-c"><% if (order[i].get('shippingStatus') === 'shipped' ) { %>已发货<% } else { %><strong>未发货</strong><% } %></td>
                    <td class="t-c small-screen-hide"><%=order[i].get('shippingDate').getFullYear()%>/<%=(order[i].get('shippingDate').getMonth()+1)%>/<%=order[i].get('shippingDate').getDate()%></td>
                    <td class="t-c small-screen-hide"><a href="/order/remove/<%= order[i].get('orderId') %>" class="remove-order">删除</a> </td>
                </tr>
                <% } %>
                </tbody>
            </table>
            
            <ul class="am-pagination">
                <% if (orderPager.prev) { %> <li>
                    <a href="/order?page=<%= orderPager.prev %><% if(searchOrderName) {%>&search-order-name=<%=searchOrderName%> <%}%><% if(searchCustomerName) {%>&search-customer-name=<%=searchCustomerName%> <%}%><% if(searchAddress) {%>&search-address=<%=searchAddress%> <%}%>">上一页</a></li> <% } %>
                <% if (orderPager.next) { %> <li>
                    <a href="/order?page=<%= orderPager.next %><% if(searchOrderName) {%>&search-order-name=<%=searchOrderName%> <%}%><% if(searchCustomerName) {%>&search-customer-name=<%=searchCustomerName%> <%}%><% if(searchAddress) {%>&search-address=<%=searchAddress%> <%}%>">下一页</a></li> <% } %>
                <li class="count">第<%=orderPager.page%>页 共<%= orderCount %>条</li>
            </ul>
            
            <hr/>

            <div class="am-form-group">
                <a href="/order/add" class="am-btn am-btn-primary" role="button">添加新订单</a>
            </div>

        </fieldset>


</div>


<div class="am-modal am-modal-confirm" tabindex="-1" id="confirm-remove-order">
    <div class="am-modal-dialog">
        <div class="am-modal-bd">
            确定要删除这条记录吗？
        </div>
        <div class="am-modal-footer">
            <span class="am-modal-btn" data-am-modal-confirm>确定</span>
            <span class="am-modal-btn" data-am-modal-cancel>取消</span>
        </div>
    </div>
</div>


<% include ../partials/footer %>
<% include ../partials/js %>

<script type="text/javascript">
    $(function() {
        var order = require('order');
        order.indexFun();
    });
</script>
</body>
</html>