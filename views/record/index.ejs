<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no"/>
    <link rel="icon" type="image/png" href="/assets/i/favicon.png">
    <title><%= title %></title>
    <% include ../partials/css %>
</head>
<body>

<% include ../partials/header %>


<div class="am-container common-page">

    <% if (flash.success != '') { %>
    <div class="am-alert am-alert-success">
        <p><%= flash.success %></p>
    </div>
    <% } %>
    
        <fieldset>

            <legend><a href="/record">产品录入列表</a></legend>

            <form class="am-form-inline" role="form" action="/record">
               
                <div class="am-form-group">
                  搜索产品：<input type="text" name="search-name" class="am-form-field" placeholder="搜索产品">
                </div>
                <button type="submit" class="am-btn am-btn-primary">搜索</button>

                <span></span>
                
                <div class="am-form-group">选择产品分类：</div>
                
                <div class="am-form-group am-form-select am-form">
                    <select class="select-record-category">
                        <option value="0">请选择产品录入分类</option>
                        <% for (var i=0;i < recordCategory.length; i++) { %>
                        <option value="<%= recordCategory[i].get('recordCategoryId'); %>"> <%= recordCategory[i].get('name'); %> </option>
                        <% } %>
                    </select>
                </div>
                
            </form>

            <hr/>
            
            <table class="am-table">
                <thead>
                <tr>
                    <th>产品名称</th>
                    <th class="t-c">产品分类</th>
                    <th class="t-c small-screen-hide">创建时间</th>
                    <th class="t-c small-screen-hide">更新时间</th>
                    <th class="t-c small-screen-hide">删除</th>
                </tr>
                </thead>
                <tbody>
                <% for(var i=0; i < record.length; i++) {%>
                <tr>
                    <td>
                        <% if(record[i].get('image')) { %> <img src="<%= record[i].get('image') %>" width="40" alt=""> <% } %>
                        <a href="/record/edit/<%= record[i].get('recordId') %>" title="<%= record[i].get('nameEn')%>"><%= record[i].get('name') %></a> </td>
                    <td class="t-c">
                        <a href="/record?recordCategoryId=<%= record[i].get('recordCategoryId') %>"><%= record[i].get('recordCategoryName') %></a>
                    </td>
                    <td class="t-c small-screen-hide"><%=(record[i].createdAt.getMonth()+1)%>-<%=record[i].createdAt.getDate()%></td>
                    <td class="t-c small-screen-hide"><%=(record[i].updatedAt.getMonth()+1)%>-<%=record[i].updatedAt.getDate()%></td>
                    <td class="t-c small-screen-hide"><a href="/record/remove/<%= record[i].get('recordId') %>" class="remove-record">删除</a> </td>
                </tr>
                <% } %>
                </tbody>
            </table>
            
            <ul class="am-pagination">
                <% if (recordPager.prev) { %> <li>
                    <a href="/record?page=<%= recordPager.prev %><% if (recordCategoryId) {%>&recordCategoryId=<%=recordCategoryId%> <%}%><% if (search) {%>&search-name=<%=search%> <%}%>">上一页</a></li> <% } %>
                <% if (recordPager.next) { %> <li>
                    <a href="/record?page=<%= recordPager.next %><% if (recordCategoryId) {%>&recordCategoryId=<%=recordCategoryId%> <%}%><% if (search) {%>&search-name=<%=search%> <%}%>">下一页</a></li> <% } %>
                <li class="count">第<%=recordPager.page%>页 共<%=recordCount%>条</li>     
            </ul>
            
            <hr/>

            <div class="am-form-group">
                <a href="/record/add" class="am-btn am-btn-primary" role="button">添加新产品</a>
            </div>

        </fieldset>

</div>


<div class="am-modal am-modal-confirm" tabindex="-1" id="confirm-remove-record">
    <div class="am-modal-dialog">
        <div class="am-modal-bd">
            确定要删除这条记录吗？
        </div>
        <div class="am-modal-footer">
            <span class="am-modal-btn" data-am-modal-confirm>确定</span>
            <span class="am-modal-btn" data-am-modal-cancel>取消</span>
        </div>
    </div>
</div>


<% include ../partials/footer %>
<% include ../partials/js %>

<script type="text/javascript">
    $(function() {
        var record = require('record');
        record.indexFun();
    });
</script>
</body>
</html>